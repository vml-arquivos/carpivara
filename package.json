{
  "name": "buscarr-platform",
  "private": true,
  "version": "0.5.0",
  "workspaces": ["apps/api", "apps/web"],
  "scripts": {
    "dev": "npm-run-all --parallel dev:api dev:web",
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "build": "npm run build -w apps/api && npm run build -w apps/web",
    "test": "npm run build && node --test apps/api/test/**/*.test.mjs",
    "test:unit": "npm run build && node --test apps/api/test/**/*.test.mjs",
    "test:integration": "./scripts/integration-smoke.sh",
    "db:migrate": "npm run build -w apps/api && node apps/api/dist/cli.js migrate",
    "db:verify": "npm run build -w apps/api && node apps/api/dist/cli.js verify",
    "release:verify": "npm run build && node --test apps/api/test/**/*.test.mjs && npm audit --omit=dev --audit-level=high && npm run release:source-check",
    "release:source-check": "node scripts/release-verify.mjs",
    "verify": "npm run release:verify",
    "start": "npm run start -w apps/api"
  },
  "devDependencies": {"npm-run-all": "^4.1.5"},
  "overrides": {"qs": "6.16.0", "body-parser": {"qs": "6.16.0"}}
}
